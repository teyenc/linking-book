import {
        SafeAreaView,
        ScrollView,
        StatusBar,
        StyleSheet,
        Text,
        useColorScheme,
        View,
        Dimensions, LogBox, Platform,
      } from 'react-native';
import React from 'react';

const test = () => {
        return (
                <View>
                        <Text style={{color: "black"}}>123678</Text>
                </View>
        )
}

export default test;